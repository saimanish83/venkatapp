import React from 'react';

const Slider = () => {
    const name = 'manish';
    const date = "2021-03-13";
    if (name === 'manish') {
      return 'manish';
    } else {
      return '';
    }
  }

export default Slider;
